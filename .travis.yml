language: python

cache: pip

python:
  - "3.6"
  - "3.7.0"

dist: xenial

install:
  - pip install codecov
  - pip install pytest-cov
  - pip install -r requirements.txt

script:
  - pytest --cov 
  - codecov
